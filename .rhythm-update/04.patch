+function normalizeDb(rows){const out=[];for(const s of rows||[]){if(Number(s.isDeleted)===1)continue;for(const d of ['easy','normal','hard','oni','ura']){const c=s.courses?.[d];if(!c||!Number(c.level))continue;out.push({key:key(s.title,d),songNo:String(s.songNo||''),title:String(s.title||''),titleKo:String(s.titleKo||''),aliasKo:String(s.aliasKo||''),artists:Array.isArray(s.artists)?s.artists.join(', '):'',genres:Array.isArray(s.genre)?s.genre.join(' / '):'',diff:d,star:Number(c.level),bpmMin:Number(s.bpm?.min)||0,bpmMax:Number(s.bpm?.max)||0,maxCombo:Number(c.maxCombo)||0,playTime:Number(c.playTime)||0,maxDensity:Number(c.maxDensity)||0,branched:!!Number(c.isBranched),balloon:Array.isArray(c.balloon)?c.balloon:[]})}}return out}
+async function loadMusic(force=false){$('taikoApiStatus').textContent='곡 데이터 불러오는 중';try{if(!force&&charts.length){render();$('taikoApiDot').className='dot ok';$('taikoApiStatus').textContent=`${charts.length.toLocaleString()} 채보 준비됨`;return}const r=await fetch(DB,{cache:force?'reload':'default'});if(!r.ok)throw Error('HTTP '+r.status);const a=normalizeDb(await r.json());if(!a.length)throw Error('데이터 비어 있음');charts=a;localStorage.setItem(KM,JSON.stringify(a));render();$('taikoApiDot').className='dot ok';$('taikoApiStatus').textContent=`${a.length.toLocaleString()} 채보 준비됨`}catch(e){$('taikoApiDot').className='dot '+(charts.length?'ok':'error');$('taikoApiStatus').textContent=charts.length?'온라인 갱신 실패 · 캐시 사용':'곡 DB 로딩 실패';render()}}
+function accuracy(r){const g=Number(r?.good)||0,o=Number(r?.ok)||0,b=Number(r?.bad)||0,t=g+o+b;return t?100*(g+o*.5)/t:null}
+function filtered(){let q=norm($('taikoSearch').value).toLowerCase(),dv=$('taikoDiff').value,st=$('taikoStatus').value,mn=Number($('taikoMinStar').value)||1,mx=Number($('taikoMaxStar').value)||10,a=charts.filter(c=>c.star>=mn&&c.star<=mx);if(q)a=a.filter(c=>(c.title+' '+c.titleKo+' '+c.aliasKo+' '+c.artists).toLowerCase().includes(q));if(dv==='oni+ura')a=a.filter(c=>['oni','ura'].includes(c.diff));else if(dv!=='all')a=a.filter(c=>c.diff===dv);a=a.filter(c=>{let r=rec(c),n=note(c);if(st==='played')return !!r?.score;if(st==='unplayed')return !r?.score;if(st==='clear')return ['silver','gold','donderfull'].includes(r?.crown);if(st==='fc')return ['gold','donderfull'].includes(r?.crown);if(st==='dfc')return r?.crown==='donderfull';if(st==='goal')return n.goal;return true});let so=$('taikoSort').value;a.sort((x,y)=>so==='title'?x.title.localeCompare(y.title,'ja'):so==='score-desc'?(rec(y)?.score||0)-(rec(x)?.score||0):so==='acc-desc'?(accuracy(rec(y))||0)-(accuracy(rec(x))||0):y.star-x.star||x.title.localeCompare(y.title,'ja'));return a}
+function traits(c){let a=[];let bpm=c.bpmMax||c.bpmMin;if(bpm>=220)a.push('고속');else if(bpm&&bpm<=130)a.push('저속');if(c.maxDensity>=8)a.push('고밀도');else if(c.maxDensity>=6)a.push('물량');if(c.maxCombo>=900)a.push('장체력');if(c.branched)a.push('보면분기');if(c.diff==='ura')a.push('우라');return a.slice(0,3)}
+function review(c){let p=[];let bpm=c.bpmMax||c.bpmMin;if(bpm>=220)p.push('빠른 BPM에서 복합을 끊지 않고 처리하는 속도 적응이 중요합니다.');if(c.maxDensity>=8)p.push('최대 밀도가 높아 순간 복합 처리와 체력 유지가 점수에 크게 영향을 줍니다.');else if(c.maxCombo>=900)p.push('노트 수가 많아 후반까지 팔 힘과 판정 안정성을 유지하는 것이 핵심입니다.');if(c.branched)p.push('보면 분기가 있어 조건에 따라 체감 패턴이 달라질 수 있습니다.');if(!p.length)p.push(`★${c.star}에서 요구되는 기본 복합과 판정력을 균형 있게 확인하기 좋은 채보입니다.`);return p.slice(0,2).join(' ')}
+function featureHTML(c){return `<div class="chartFeatureTitle">채보 특징 · ${courseNames[c.diff]} ★${c.star}</div><div class="chartTraits">${traits(c).map(x=>`<span class="traitChip hot">${x}</span>`).join('')}</div><div class="noteBreakdown"><div class="noteCell"><b>${c.maxCombo||'—'}</b><span>최대 콤보</span></div><div class="noteCell"><b>${c.maxDensity?c.maxDensity.toFixed(2):'—'}</b><span>최대 밀도</span></div><div class="noteCell"><b>${c.bpmMin||'—'}${c.bpmMax&&c.bpmMax!==c.bpmMin?'~'+c.bpmMax:''}</b><span>BPM</span></div><div class="noteCell"><b>${c.branched?'있음':'없음'}</b><span>보면 분기</span></div><div class="noteCell"><b>${c.balloon?.length||0}</b><span>풍선 구간</span></div></div><div class="chartReview taikoReview"><b>보면 총평</b><br>${esc(review(c))}</div>`}
+function crownCls(r){return r?.crown?'crown-'+r.crown:''}
+function cards(){const a=filtered(),p=a.filter(c=>rec(c)?.score),cl=p.filter(c=>['silver','gold','donderfull'].includes(rec(c).crown)).length,fc=p.filter(c=>['gold','donderfull'].includes(rec(c).crown)).length,dfc=p.filter(c=>rec(c).crown==='donderfull').length,accs=p.map(c=>accuracy(rec(c))).filter(x=>x!=null),av=accs.length?accs.reduce((s,x)=>s+x,0)/accs.length:null,t10=p.filter(c=>c.star===10&&['silver','gold','donderfull'].includes(rec(c).crown)).length;$('taikoStatTotal').textContent=a.length;$('taikoStatPlayed').textContent=p.length;$('taikoStatClear').textContent=cl;$('taikoStatFcDfc').textContent=`${fc} / ${dfc}`;$('taikoStatAcc').textContent=av!=null?av.toFixed(2)+'%':'—';$('taikoStat10').textContent=t10;$('taikoContent').innerHTML=a.length?`<div class="moduleCardGrid">${a.map(c=>{let r=rec(c),n=note(c),ac=accuracy(r);return `<article class="moduleSong taikoSong ${crownCls(r)} ${r?.badge?'badge-'+r.badge:''}" data-tkey="${esc(c.key)}"><div class="moduleSongTop"><div class="moduleLevel">★${c.star}</div><div class="moduleDiff">${courseNames[c.diff]}</div></div><div class="moduleTitle"><a href="${yt(c)}" target="_blank" rel="noopener" onclick="event.stopPropagation()">${esc(c.titleKo||c.title)} ▶</a></div><div class="moduleArtist">${esc(c.titleKo?c.title:'')} ${esc(c.artists)}</div><div class="moduleScore">${r?r.score.toLocaleString():'—'}</div><div class="moduleBadges">${r?.crown?`<span class="mBadge">${crownLabel[r.crown]||r.crown}</span>`:''}${r?.badge?`<span class="mBadge">${r.badge}</span>`:''}${ac!=null?`<span class="mBadge">정확도 ${ac.toFixed(2)}%</span>`:''}${n.goal?'<span class="mBadge">⭐ 목표</span>':''}</div><div class="chartTraits">${traits(c).map(x=>`<span class="traitChip hot">${x}</span>`).join('')}</div><div class="moduleMinor">BPM ${c.bpmMin||'—'}${c.bpmMax&&c.bpmMax!==c.bpmMin?'~'+c.bpmMax:''}${n.memo?' · 📝 '+esc(n.memo):''}</div></article>`}).join('')}</div>`:'<div class="empty"><b>조건에 맞는 채보가 없습니다.</b></div>';document.querySelectorAll('[data-tkey]').forEach(e=>e.onclick=()=>openEdit(e.dataset.tkey))}
+function highRecs(limit=8){let p=charts.filter(c=>['oni','ura'].includes(c.diff)&&rec(c)?.score);return p.map(c=>{let r=rec(c),peers=p.filter(x=>x.key!==c.key&&x.star===c.star&&rec(x)?.score),avg=peers.length?peers.reduce((s,x)=>s+rec(x).score,0)/peers.length:r.score,recover=Math.max(0,avg-r.score),lamp=r.crown==='gold'?'전량':r.crown==='silver'?'풀콤보':r.crown==='donderfull'?'고점 회수':'클리어',priority=(recover/5000)+(r.crown==='played'?3:r.crown==='silver'?2:r.crown==='gold'?1:0)+c.star*.03;return{c,r,recover,lamp,priority}}).filter(x=>x.recover>1000||x.r.crown!=='donderfull').sort((a,b)=>b.priority-a.priority).slice(0,limit)}
+function practiceRecs(limit=8){let p=charts.filter(c=>['oni','ura'].includes(c.diff)&&rec(c)?.score);if(p.length<4)return[];let withAcc=p.map(c=>({c,r:rec(c),a:accuracy(rec(c))})).filter(x=>x.a!=null),base=withAcc.length?withAcc.reduce((s,x)=>s+x.a,0)/withAcc.length:null;return p.map(c=>{let r=rec(c),a=accuracy(r),reason='',score=0;if(a!=null&&base!=null){score=base-a;reason=`전체 평균보다 정확도 ${(base-a).toFixed(2)}%p 낮음`}else{let peers=p.filter(x=>x.star===c.star),avg=peers.reduce((s,x)=>s+rec(x).score,0)/Math.max(1,peers.length);score=(avg-r.score)/10000;reason=`같은 ★${c.star} 평균 대비 ${Math.max(0,Math.round(avg-r.score)).toLocaleString()}점 회수 여지`}return{c,r,score,reason}}).sort((a,b)=>b.score-a.score).slice(0,limit)}
+function recBox(title,sub,a,kind){if(!a.length)return'';return `<div class="recommendBox"><h2>${title}</h2><div class="recommendSub">${sub}</div><div class="recommendGrid">${a.map((x,i)=>`<a class="recommendItem" href="${yt(x.c)}" target="_blank" rel="noopener"><div class="recommendTop"><div class="recommendTitle">${i+1}. ${esc(x.c.titleKo||x.c.title)}</div><div class="recommendValue">${kind==='high'?x.lamp:'연습'}</div></div><div class="recommendReason">${kind==='high'?(x.recover>1000?`같은 ★${x.c.star} 평균보다 약 ${Math.round(x.recover).toLocaleString()}점 낮아 회수 여지 · 현재 ${crownLabel[x.r.crown]||'플레이'}`:`현재 ${crownLabel[x.r.crown]||'플레이'} → ${x.lamp} 목표`):x.reason}</div><div class="recommendBadges"><span class="recommendBadge gain">★${x.c.star}</span>${traits(x.c).map(t=>`<span class="recommendBadge">${t}</span>`).join('')}</div></a>`).join('')}</div></div>`}
+function summary(){let p=charts.filter(c=>['oni','ura'].includes(c.diff)&&rec(c)?.score),fc=p.filter(c=>['gold','donderfull'].includes(rec(c).crown)).length,df=p.filter(c=>rec(c).crown==='donderfull').length,hfc=p.filter(c=>['gold','donderfull'].includes(rec(c).crown)).sort((a,b)=>b.star-a.star)[0],hdf=p.filter(c=>rec(c).crown==='donderfull').sort((a,b)=>b.star-a.star)[0];$('taikoSummary').innerHTML=`<div class="moduleSummaryGrid"><div class="moduleBox"><h2>플레이 채보</h2><div class="moduleMetric">${p.length}</div><div class="moduleMetricSmall">오니/우라 최고기록</div></div><div class="moduleBox"><h2>풀콤 / 전량</h2><div class="moduleMetric">${fc} / ${df}</div><div class="moduleMetricSmall">금왕관 / 전량</div></div><div class="moduleBox"><h2>최고 풀콤</h2><div class="moduleMetric">${hfc?'★'+hfc.star:'—'}</div><div class="moduleMetricSmall">${esc(hfc?.titleKo||hfc?.title||'기록 없음')}</div></div><div class="moduleBox"><h2>최고 전량</h2><div class="moduleMetric">${hdf?'★'+hdf.star:'—'}</div><div class="moduleMetricSmall">${esc(hdf?.titleKo||hdf?.title||'기록 없음')}</div></div></div>${recBox('고점 올리기 좋은 보면','같은 ★난도에서 네 평균보다 낮거나 다음 왕관 갱신이 가까운 채보.',highRecs(),'high')}`}
+function recommend(){$('taikoRecommend').innerHTML=recBox('고점 올리기 좋은 보면','다음 왕관·개인 최고점 회수에 적합한 오니/우라.',highRecs(),'high')+recBox('연습 추천곡','현재 기록에서 상대적으로 약하게 나온 보면을 우선 추천합니다.',practiceRecs(),'practice')||'<div class="empty"><b>기록이 더 필요합니다.</b></div>'}
+function analysis(){let p=charts.filter(c=>['oni','ura'].includes(c.diff)&&rec(c)?.score);if(p.length<5){$('taikoAnalysis').innerHTML='<div class="empty"><b>오니/우라 기록 5개 이상이 필요합니다.</b></div>';return}const defs=[['고속',c=>(c.bpmMax||c.bpmMin)>=220],['저속',c=>(c.bpmMax||c.bpmMin)>0&&(c.bpmMax||c.bpmMin)<=130],['고밀도',c=>c.maxDensity>=8],['장체력',c=>c.maxCombo>=900],['보면분기',c=>c.branched],['우라',c=>c.diff==='ura']],base=p.map(c=>accuracy(rec(c))).filter(x=>x!=null),baseAvg=base.length?base.reduce((s,x)=>s+x,0)/base.length:null,out=[];for(const [name,fn] of defs){let a=p.filter(fn),vals=a.map(c=>accuracy(rec(c))).filter(x=>x!=null);if(a.length<2)continue;if(vals.length&&baseAvg!=null)out.push({name,n:a.length,res:vals.reduce((s,x)=>s+x,0)/vals.length-baseAvg});else{let av=a.reduce((s,c)=>s+rec(c).score,0)/a.length,all=p.reduce((s,c)=>s+rec(c).score,0)/p.length;out.push({name,n:a.length,res:(av-all)/10000})}}out.sort((a,b)=>b.res-a.res);$('taikoAnalysis').innerHTML=recBox('연습 추천곡','약점 경향과 개인 기록 회수 여지를 함께 보는 추천.',practiceRecs(),'practice')+`<div class="panel notice"><b>성향 분석:</b> 상세 판정(良/可/不可)이 있으면 정확도, 없으면 같은 기록군의 점수 편차를 사용합니다.</div><div class="moduleAnalysisGrid"><div class="moduleBox"><h2>상대적 강점</h2>${out.filter(x=>x.res>0).slice(0,4).map(x=>`<div class="traitRow"><span>${x.name} <span class="genrePill">${x.n}곡</span></span><b class="pos">+${x.res.toFixed(2)}</b></div>`).join('')||'—'}</div><div class="moduleBox"><h2>상대적 약점</h2>${[...out].reverse().filter(x=>x.res<0).slice(0,4).map(x=>`<div class="traitRow"><span>${x.name} <span class="genrePill">${x.n}곡</span></span><b class="neg">${x.res.toFixed(2)}</b></div>`).join('')||'—'}</div></div>`}
+function render(){cards();summary();recommend();analysis();refreshHub()}
+function openEdit(k){let c=charts.find(x=>x.key===k);if(!c)return;editing=k;let r=rec(c),n=note(c);$('taikoEditTitle').textContent=c.titleKo||c.title;$('taikoEditMeta').textContent=`${courseNames[c.diff]} · ★${c.star} · ${c.title}`;$('taikoEditScore').value=r?.score||'';$('taikoEditCrown').value=r?.crown||'';$('taikoEditGoal').value=n.goal?'1':'0';$('taikoEditGood').value=r?.good??'';$('taikoEditOk').value=r?.ok??'';$('taikoEditBad').value=r?.bad??'';$('taikoEditMemo').value=n.memo||'';$('taikoEditFeatures').innerHTML=featureHTML(c);$('taikoEditDialog').showModal()}
+function importText(t){let ok=0;for(const ln of String(t||'').split(/\r?\n/)){if(!ln.trim()||ln.startsWith('#'))continue;const p=ln.split('\t');if(p.length<3)continue;let title=norm(p[0]),diff=String(p[1]||'').toLowerCase(),score=Number(String(p[2]||0).replace(/,/g,''));if(!['easy','normal','hard','oni','ura'].includes(diff)||!score)continue;let c=charts.find(x=>x.key===key(title,diff)||norm(x.titleKo).toLowerCase()===title.toLowerCase()&&x.diff===diff);let crown=String(p[3]||'').toLowerCase(),badge=String(p[4]||'').toLowerCase();if(!['played','silver','gold','donderfull'].includes(crown))crown='played';let r={title:c?.title||title,diff,score,crown,badge,good:Number(p[5])||0,ok:Number(p[6])||0,bad:Number(p[7])||0,maxCombo:Number(p[8])||0,roll:Number(p[9])||0,playCount:Number(p[10])||0};let k=c?.key||key(r.title,diff),prev=records.get(k);if(!prev||score>=prev.score)records.set(k,{...prev,...r});else records.set(k,{...prev,crown:rankCrown(crown)>rankCrown(prev.crown)?crown:prev.crown});ok++}save();render();return ok}
+const rankCrown=c=>({donderfull:4,gold:3,silver:2,played:1}[c]||0);
+function backup(){let o={app:'Rhythm Archive',game:'TAIKO',records:[...records.values()],notes:[...notes.entries()].map(([key,v])=>({key,...v}))};let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(o,null,2)],{type:'application/json'}));a.download='taiko_rhythm_archive_backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
+const BOOKMARK=`javascript:(async()=>{if(location.host!=='donderhiroba.jp'){alert('동더히로바에서 실행해주세요.');return}let B=document.getElementById('rh-taiko-box');B?.remove();B=document.createElement('div');B.id='rh-taiko-box';B.style='position:fixed;z-index:999999;left:10px;right:10px;top:10px;max-height:90vh;overflow:auto;background:#2a1714;color:white;border:2px solid #ff7866;border-radius:12px;padding:12px;font:14px sans-serif';B.innerHTML='<b>태고 기록 수집 중…</b><div id="rtm">최근 이력 1페이지</div>';document.body.appendChild(B);const O=[],seen=new Set(),D=c=>c==='5'?'ura':c==='4'?'oni':c==='3'?'hard':c==='2'?'normal':'easy',C=x=>({'01':'played','03':'silver','02':'gold','04':'donderfull'}[x]||'played'),BG=x=>({8:'rainbow',7:'purple',6:'pink',5:'gold',4:'silver',3:'bronze',2:'white'}[x]||'');try{for(let page=1;page<=100;page++){document.getElementById('rtm').textContent='최근 이력 '+page+'페이지 읽는 중…';const h=await(await fetch('/history_recent_score.php?page='+page,{credentials:'include',cache:'no-store'})).text(),d=new DOMParser().parseFromString(h,'text/html'),els=[...d.querySelectorAll('.scoreUser')];if(!els.length)break;let sig='';for(const e of els){const n=(e.querySelector('.songNameTitleScore h2')?.textContent||'').trim(),ds=e.querySelector('.levelIcon')?.getAttribute('src')||'',dc=(/icon_course02_(\\d+)_640/.exec(ds)||[])[1]||'1',df=D(dc),cr=e.querySelectorAll('.crownIcon'),cc=(/crown_(\\d+)_640/.exec(cr[0]?.getAttribute('src')||'')||[])[1],bi=(/best_score_rank_(\\d+)_640/.exec(cr[1]?.getAttribute('src')||'')||[])[1],sc=Number((e.querySelector('.scoreScore')?.textContent||'').replace(/[^0-9]/g,''))||0,v=[...e.querySelectorAll('.scoreDataArea .playDataScore')].map(x=>Number((x.textContent||'').replace(/[^0-9]/g,''))||0);if(!n||!sc)continue;sig+=n+'|'+df+';';const k=n+'|'+df;if(seen.has(k))continue;seen.add(k);O.push([n,df,sc,C(cc),BG(Number(bi)),v[0]||0,v[2]||0,v[4]||0,v[1]||0,v[3]||0,v[5]||0].join('\\t'))}if(!sig||page>1&&sig===window.__rhTaikoPrev)break;window.__rhTaikoPrev=sig;await new Promise(r=>setTimeout(r,120))}const v=O.join('\\n');B.innerHTML='<b>수집 완료: '+O.length+'개 기록</b><textarea id="rtt" style="width:100%;height:220px;margin-top:8px"></textarea><div style="display:flex;gap:8px;margin-top:8px"><button id="rtc">기록 복사</button><button id="rtx">닫기</button></div><div id="rts"></div>';document.getElementById('rtt').value=v;document.getElementById('rtc').onclick=async()=>{const t=document.getElementById('rtt');try{await navigator.clipboard.writeText(t.value)}catch{t.select();document.execCommand('copy')}document.getElementById('rts').textContent='복사 완료! Rhythm Archive에 붙여넣으세요.'};document.getElementById('rtx').onclick=()=>B.remove()}catch(e){document.getElementById('rtm').textContent='실패: '+e.message}})()`;
+function copy(id){let t=$(id);t.focus();t.select();t.setSelectionRange(0,t.value.length);if(navigator.clipboard&&window.isSecureContext)navigator.clipboard.writeText(t.value).catch(()=>document.execCommand('copy'));else document.execCommand('copy')}
+function refreshHub(){const e=$('hubTaikoRating'),m=$('hubTaikoMeta');if(!e||!m)return;let p=charts.filter(c=>['oni','ura'].includes(c.diff)&&rec(c)?.score),fc=p.filter(c=>['gold','donderfull'].includes(rec(c).crown)).length,df=p.filter(c=>rec(c).crown==='donderfull').length;e.textContent=p.length?`★10 ${p.filter(c=>c.star===10&&['silver','gold','donderfull'].includes(rec(c).crown)).length}`:'—';m.textContent=`저장 기록 ${records.size}채보 · FC ${fc} · 전량 ${df}`}
+window.__taikoRefreshHub=refreshHub;loadLocal();loadMusic(false);
