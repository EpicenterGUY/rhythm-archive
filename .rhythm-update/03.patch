+async function loadMusic(force=false){$('sdvxApiStatus').textContent='곡 데이터 불러오는 중';try{if(!force&&charts.length){mergeRecordCharts();render();$('sdvxApiDot').className='dot ok';$('sdvxApiStatus').textContent=`${charts.length.toLocaleString()} 채보 준비됨`;return}const r=await fetch(API,{cache:force?'reload':'default'});if(!r.ok)throw Error('HTTP '+r.status);const a=normalize(await r.json());if(!a.length)throw Error('데이터 비어 있음');charts=a;localStorage.setItem(KM,JSON.stringify(a));mergeRecordCharts();render();$('sdvxApiDot').className='dot ok';$('sdvxApiStatus').textContent=`${charts.length.toLocaleString()} 채보 준비됨`}catch(e){mergeRecordCharts();$('sdvxApiDot').className='dot '+(charts.length?'ok':'error');$('sdvxApiStatus').textContent=charts.length?'공개 DB 연결 실패 · 기록/캐시 사용':'기록을 가져오면 바로 사용 가능';render()}}
+function filtered(){let q=norm($('sdvxSearch').value).toLowerCase(),dv=$('sdvxDiff').value,st=$('sdvxStatus').value,mn=Number($('sdvxMinLv').value)||0,mx=Number($('sdvxMaxLv').value)||99,a=charts.filter(c=>c.level>=mn&&c.level<=mx);if(q)a=a.filter(c=>(c.title+' '+c.artist).toLowerCase().includes(q));if(dv==='high')a=a.filter(c=>c.level>=17);else if(dv==='MXM+')a=a.filter(c=>!['NOV','ADV','EXH'].includes(c.diff));a=a.filter(c=>{let r=rec(c),n=note(c);if(st==='played')return !!r?.score;if(st==='unplayed')return !r?.score;if(st==='s')return r?.score>=9900000;if(st==='exc')return ['EXC','MXC','UC','PUC'].includes(r?.lamp);if(st==='uc')return ['UC','PUC'].includes(r?.lamp);if(st==='puc')return r?.lamp==='PUC';if(st==='goal')return n.goal;return true});const so=$('sdvxSort').value;a.sort((x,y)=>so==='title'?x.title.localeCompare(y.title,'ja'):so==='score-desc'?(rec(y)?.score||0)-(rec(x)?.score||0):so==='vf-desc'?force(y.level,rec(y)?.score,rec(y)?.lamp)-force(x.level,rec(x)?.score,rec(x)?.lamp):y.level-x.level||x.title.localeCompare(y.title,'ja'));return a}
+function radarTraits(c){const r=c.radar||{},pairs=[['노브',r.tsumami],['원핸드',r.oneHand],['출장',r.handTrip],['트릭',r.tricky],['물량',r.notes],['피크',r.peak]].filter(x=>x[1]>0).sort((a,b)=>b[1]-a[1]);return pairs.slice(0,3).map(x=>x[0])}
+function review(c){const r=c.radar||{},t=radarTraits(c),parts=[];if(t.includes('노브'))parts.push('아날로그 디바이스 비중이 높아 노브 추적과 버튼 동시처리가 핵심입니다.');if(t.includes('원핸드'))parts.push('원핸드 처리 비중이 높아 한 손으로 버튼·FX를 안정적으로 묶는 능력이 중요합니다.');if(t.includes('출장'))parts.push('반대편 입력을 잡는 출장/교차 동작이 자주 요구되는 타입입니다.');if(t.includes('트릭'))parts.push('변속·시점·낚시성 패턴 등 트릭 대응력이 점수 차이를 만들기 쉽습니다.');if(t.includes('물량')||t.includes('피크'))parts.push('밀도와 순간 피크가 높아 후반까지 손을 유지하는 체력과 정확도가 중요합니다.');if(!parts.length)parts.push(`${c.level.toFixed(1)} 레벨의 기본 처리력을 균형 있게 요구하는 채보입니다.`);return parts.slice(0,2).join(' ')}
+function featureHTML(c){const r=c.radar||{},mx=Math.max(200,...Object.values(r).map(Number).filter(Number.isFinite));const cells=[['NOTES',r.notes],['PEAK',r.peak],['TSUMAMI',r.tsumami],['ONE-HAND',r.oneHand],['HAND-TRIP',r.handTrip],['TRICKY',r.tricky]];return `<div class="chartFeatureTitle">채보 특징 · ${esc(c.diff)} ${c.level.toFixed(1)}</div><div class="chartTraits">${radarTraits(c).map(x=>`<span class="traitChip tech">${x}</span>`).join('')}</div><div class="radarBars">${cells.map(([n,v])=>`<div class="radarBar">${n} ${v||0}<i style="--pct:${Math.min(100,(Number(v)||0)/mx*100)}%"></i></div>`).join('')}</div><div class="chartReview sdvxReview"><b>보면 총평</b><br>${esc(review(c))}</div><div class="featureExplain">BPM ${esc(c.bpm||'—')} · Effected by <b>${esc(c.effector||'—')}</b></div>`}
+function vfData(){const a=charts.map(c=>{const r=rec(c);return r?.score?{c,r,v:force(c.level,r.score,r.lamp)}:null}).filter(Boolean).sort((a,b)=>b.v-a.v||b.r.score-a.r.score);return{all:a,top:a.slice(0,50),vf:a.slice(0,50).reduce((s,x)=>s+x.v,0),cut:a.length>=50?a[49].v:0}}
+function cards(){const a=filtered(),p=a.filter(c=>rec(c)?.score),sct=p.filter(c=>rec(c).score>=9900000).length,uc=p.filter(c=>['UC','PUC'].includes(rec(c).lamp)).length,puc=p.filter(c=>rec(c).lamp==='PUC').length,av=p.length?Math.round(p.reduce((z,c)=>z+rec(c).score,0)/p.length):0,v=vfData();$('sdvxStatTotal').textContent=a.length;$('sdvxStatPlayed').textContent=p.length;$('sdvxStatS').textContent=sct;$('sdvxStatUcPuc').textContent=`${uc} / ${puc}`;$('sdvxStatAvg').textContent=av?av.toLocaleString():'—';$('sdvxStatVf').textContent=v.vf?v.vf.toFixed(3):'—';$('sdvxContent').innerHTML=a.length?`<div class="moduleCardGrid">${a.map(c=>{let r=rec(c),n=note(c),g=r?grade(r.score):'',lamp=r?.lamp||'',cls=r?.score?'played '+(g==='S'?'grade-s':g.startsWith('AAA')?'grade-aaa':'')+' '+(lamp==='PUC'?'lamp-puc':lamp==='UC'?'lamp-uc':lamp==='EXC'||lamp==='MXC'?'lamp-exc':''):'';return `<article class="moduleSong sdvxSong ${cls}" data-skey="${esc(c.key)}"><div class="moduleSongTop"><div class="moduleLevel">${c.level.toFixed(1)}</div><div class="moduleDiff">${esc(c.diff)}</div></div><div class="moduleTitle"><a href="${yt(c)}" target="_blank" rel="noopener" onclick="event.stopPropagation()">${esc(c.title)} ▶</a></div><div class="moduleArtist">${esc(c.artist)}</div><div class="moduleScore">${r?r.score.toLocaleString():'—'}</div><div class="moduleBadges">${g?`<span class="mBadge">${g}</span>`:''}${lamp?`<span class="mBadge">${lamp}</span>`:''}${r?`<span class="mBadge">VF ${force(c.level,r.score,lamp).toFixed(3)}</span>`:''}${n.goal?'<span class="mBadge">⭐ 목표</span>':''}</div><div class="chartTraits">${radarTraits(c).map(x=>`<span class="traitChip tech">${x}</span>`).join('')}</div><div class="moduleMinor">BPM ${esc(c.bpm||'—')}${n.memo?' · 📝 '+esc(n.memo):''}</div></article>`}).join('')}</div>`:'<div class="empty"><b>조건에 맞는 채보가 없습니다.</b>SDVX 기록 가져오기에서 TSV/JSON을 넣으면 기록 중심으로 바로 사용할 수 있습니다.</div>';document.querySelectorAll('[data-skey]').forEach(e=>e.onclick=()=>openEdit(e.dataset.skey))}
+const scoreTargets=[[6500000,'C'],[7500000,'B'],[8700000,'A'],[9000000,'A+'],[9300000,'AA'],[9500000,'AA+'],[9700000,'AAA'],[9800000,'AAA+'],[9900000,'S'],[10000000,'PUC']];
+function highRecs(limit=6){return charts.map(c=>{let r=rec(c);if(!r?.score||r.score>=10000000)return null;let n=scoreTargets.find(x=>x[0]>r.score);if(!n)return null;let delta=n[0]-r.score,peers=charts.filter(x=>x.key!==c.key&&Math.abs(x.level-c.level)<=.2&&rec(x)?.score),avg=peers.length?peers.reduce((s,x)=>s+rec(x).score,0)/peers.length:r.score,recover=Math.max(0,avg-r.score);return{c,r,target:n[0],label:n[1],delta,recover,score:1/(delta/10000+.5)+recover/300000}}).filter(Boolean).sort((a,b)=>b.score-a.score).slice(0,limit)}
+function vfRecs(limit=8){const vd=vfData();return charts.map(c=>{let r=rec(c);if(!r?.score)return null;let cand=[];let ng=scoreTargets.find(x=>x[0]>r.score);if(ng)cand.push({label:ng[1],score:ng[0],lamp:r.lamp});const lamps=['COMP','EXC','MXC','UC','PUC'],li=lamps.indexOf(r.lamp);if(li>=0&&li<lamps.length-1)cand.push({label:lamps[li+1],score:r.score,lamp:lamps[li+1]});let best=cand.map(x=>({...x,v:force(c.level,x.score,x.lamp)})).sort((a,b)=>b.v-a.v)[0];if(!best)return null;let cur=force(c.level,r.score,r.lamp),gain=Math.max(0,best.v-Math.max(cur,vd.cut));return gain>0?{c,r,best,cur,gain}:null}).filter(Boolean).sort((a,b)=>b.gain-a.gain).slice(0,limit)}
+function recHTML(title,sub,a,type){if(!a.length)return'';return `<div class="recommendBox"><h2>${title}</h2><div class="recommendSub">${sub}</div><div class="recommendGrid">${a.map((x,i)=>`<a class="recommendItem" href="${yt(x.c)}" target="_blank" rel="noopener"><div class="recommendTop"><div class="recommendTitle">${i+1}. ${esc(x.c.title)} <span class="genrePill">${x.c.diff}</span></div><div class="recommendValue">${type==='vf'?'+'+x.gain.toFixed(3)+' VF':x.label}</div></div><div class="recommendReason">${type==='vf'?`현재 ${x.cur.toFixed(3)} → ${x.best.v.toFixed(3)} · 목표 ${x.best.label}`:`현재 ${x.r.score.toLocaleString()} → ${x.target.toLocaleString()} · +${x.delta.toLocaleString()}점${x.recover>5000?' · 같은 레벨대 회수 여지':''}`}</div><div class="recommendBadges"><span class="recommendBadge gain">Lv ${x.c.level.toFixed(1)}</span>${radarTraits(x.c).slice(0,2).map(t=>`<span class="recommendBadge">${t}</span>`).join('')}</div></a>`).join('')}</div></div>`}
+function summary(){const vd=vfData(),p=vd.all,pu=p.filter(x=>x.r.lamp==='PUC').length,uc=p.filter(x=>['UC','PUC'].includes(x.r.lamp)).length,hi=p.filter(x=>x.r.score>=9900000).sort((a,b)=>b.c.level-a.c.level)[0];$('sdvxSummary').innerHTML=`<div class="moduleSummaryGrid"><div class="moduleBox"><h2>VOLFORCE</h2><div class="moduleMetric">${vd.vf?vd.vf.toFixed(3):'—'}</div><div class="moduleMetricSmall">${vd.top.length}/50 · 컷 ${vd.cut.toFixed(3)}</div></div><div class="moduleBox"><h2>고정밀</h2><div class="moduleMetric">${pu}</div><div class="moduleMetricSmall">PUC · UC 이상 ${uc}</div></div><div class="moduleBox"><h2>최고 S</h2><div class="moduleMetric">${hi?hi.c.level.toFixed(1):'—'}</div><div class="moduleMetricSmall">${esc(hi?.c.title||'기록 없음')}</div></div><div class="moduleBox"><h2>플레이 채보</h2><div class="moduleMetric">${p.length}</div><div class="moduleMetricSmall">저장된 최고기록</div></div></div>${recHTML('고점 올리기 좋은 보면','다음 스코어 그레이드가 가깝거나 같은 레벨대에서 회수 여지가 큰 채보.',highRecs(),'score')}`}
+function rating(){const vd=vfData(),rows=vd.top.map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.c.title)} <span class="genrePill">${x.c.diff}</span></td><td class="num">${x.c.level.toFixed(1)}</td><td class="num">${x.r.score.toLocaleString()}<div class="micro">${grade(x.r.score)} · ${x.r.lamp}</div></td><td class="num"><b>${x.v.toFixed(3)}</b></td></tr>`).join('');$('sdvxRating').innerHTML=recHTML('VOLFORCE 올리기 좋은 보면','현재 B50 컷을 실제로 넘길 수 있는 스코어/램프 갱신 후보.',vfRecs(),'vf')+`<div class="moduleBox"><h2>VOLFORCE 대상곡</h2><div class="moduleMetric">${vd.vf.toFixed(3)}</div><table class="moduleTable"><thead><tr><th>#</th><th>채보</th><th class="num">Lv</th><th class="num">기록</th><th class="num">FORCE</th></tr></thead><tbody>${rows||'<tr><td colspan="5">기록 없음</td></tr>'}</tbody></table></div>`}
+function analysis(){const played=charts.filter(c=>rec(c)?.score&&Object.values(c.radar||{}).some(Boolean));if(played.length<5){$('sdvxAnalysis').innerHTML='<div class="empty"><b>패턴 데이터가 있는 기록이 더 필요합니다.</b>FairyJoke 데이터가 연결되거나 레이더 정보가 있는 채보 5개 이상이면 분석합니다.</div>';return}const attrs=[['노브','tsumami'],['원핸드','oneHand'],['출장','handTrip'],['트릭','tricky'],['물량','notes'],['피크','peak']],out=[];for(const [name,k] of attrs){let a=played.filter(c=>(c.radar?.[k]||0)>0).map(c=>({c,val:c.radar[k],score:rec(c).score})).sort((x,y)=>y.val-x.val).slice(0,Math.max(3,Math.ceil(played.length*.35)));if(!a.length)continue;let avg=a.reduce((s,x)=>s+x.score,0)/a.length,all=played.reduce((s,c)=>s+rec(c).score,0)/played.length;out.push({name,n:a.length,res:avg-all})}out.sort((a,b)=>b.res-a.res);$('sdvxAnalysis').innerHTML=`<div class="panel notice"><b>패턴 성향:</b> FairyJoke 레이더 상위 채보에서 네 스코어가 전체 평균보다 얼마나 높고 낮은지 비교합니다. 손배치 판정이 아니라 경향 분석입니다.</div><div class="moduleAnalysisGrid"><div class="moduleBox"><h2>상대적 강점</h2>${out.filter(x=>x.res>0).slice(0,4).map(x=>`<div class="traitRow"><span>${x.name} <span class="genrePill">${x.n}곡</span></span><b class="pos">+${Math.round(x.res).toLocaleString()}</b></div>`).join('')||'—'}</div><div class="moduleBox"><h2>상대적 약점</h2>${[...out].reverse().filter(x=>x.res<0).slice(0,4).map(x=>`<div class="traitRow"><span>${x.name} <span class="genrePill">${x.n}곡</span></span><b class="neg">${Math.round(x.res).toLocaleString()}</b></div>`).join('')||'—'}</div></div>`}
+function render(){mergeRecordCharts();cards();summary();rating();analysis();refreshHub()}
+function openEdit(k){let c=charts.find(x=>x.key===k);if(!c)return;editing=k;let r=rec(c),n=note(c);$('sdvxEditTitle').textContent=c.title;$('sdvxEditMeta').textContent=`${c.diff} · Lv ${c.level.toFixed(1)} · VF ${r?force(c.level,r.score,r.lamp).toFixed(3):'—'}`;$('sdvxEditLevel').value=c.level;$('sdvxEditScore').value=r?.score||'';$('sdvxEditLamp').value=r?.lamp||'';$('sdvxEditGoal').value=n.goal?'1':'0';$('sdvxEditMemo').value=n.memo||'';$('sdvxEditFeatures').innerHTML=featureHTML(c);$('sdvxEditDialog').showModal()}
+function importText(txt){let arr=[];const t=String(txt||'').trim();try{const j=JSON.parse(t);arr=Array.isArray(j)?j:(j.records||j.scores||[])}catch{for(const ln of t.split(/\r?\n/)){if(!ln.trim()||ln.trim().startsWith('#'))continue;const p=ln.includes('\t')?ln.split('\t'):ln.split(',');if(p.length<4)continue;arr.push({title:p[0],diff:p[1],level:p[2],score:p[3],lamp:p[4],artist:p[5],bpm:p[6]})}}let ok=0;for(const x of arr){let title=norm(x.title||x.song),diff=String(x.diff||x.difficulty||'').toUpperCase(),lv=Number(x.level||x.lv),score=Number(String(x.score||0).replace(/,/g,'')),lamp=String(x.lamp||x.clear||'COMP').toUpperCase();if(!title||!diff||!score)continue;if(!['PUC','UC','MXC','EXC','COMP','CRASH'].includes(lamp))lamp='COMP';records.set(key(title,diff),{title,diff,level:Number.isFinite(lv)?lv:0,score:Math.min(10000000,Math.round(score)),lamp,artist:String(x.artist||''),bpm:String(x.bpm||'')});ok++}save();mergeRecordCharts();render();return ok}
+function backup(){let o={app:'Rhythm Archive',game:'SOUND VOLTEX',exportedAt:new Date().toISOString(),records:[...records.values()],notes:[...notes.entries()].map(([key,v])=>({key,...v}))};let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(o,null,2)],{type:'application/json'}));a.download='sdvx_rhythm_archive_backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
+function refreshHub(){const el=$('hubSdvxRating'),m=$('hubSdvxMeta');if(!el||!m)return;const v=vfData();el.textContent=v.vf?v.vf.toFixed(3):'—';m.textContent=`저장 기록 ${records.size}채보 · ${v.top.length}/50 VF 대상`}
+loadLocal();loadMusic(false);
+['sdvxSearch','sdvxDiff','sdvxStatus','sdvxMinLv','sdvxMaxLv','sdvxSort'].forEach(id=>$(id)?.addEventListener('input',cards));$('sdvxReload').onclick=()=>loadMusic(true);$('openSdvxImport').onclick=()=>{$('sdvxPaste').value='';$('sdvxImportResult').textContent='아직 적용하지 않았습니다.';$('sdvxImportDialog').showModal()};$('closeSdvxImport').onclick=()=>$('sdvxImportDialog').close();$('applySdvxPaste').onclick=()=>{let n=importText($('sdvxPaste').value);$('sdvxImportResult').textContent=`${n}개 기록을 적용했습니다.`};$('sdvxExport').onclick=backup;$('sdvxBackupFile').onchange=async e=>{const f=e.target.files?.[0];if(!f)return;let n=importText(await f.text());alert(n+'개 기록 적용')};$('sdvxClear').onclick=()=>{if(confirm('SDVX 기록/메모를 초기화할까요?')){records.clear();notes.clear();save();render()}};$('sdvxRandom').onclick=()=>{let a=filtered();if(a.length){let c=a[Math.floor(Math.random()*a.length)];openEdit(c.key)}};
+$('sdvxEditCancel').onclick=()=>$('sdvxEditDialog').close();$('sdvxEditSave').onclick=()=>{let c=charts.find(x=>x.key===editing);if(!c)return;let lv=Number($('sdvxEditLevel').value)||c.level,score=Number($('sdvxEditScore').value)||0,lamp=$('sdvxEditLamp').value;if(score)records.set(c.key,{...(rec(c)||{}),title:c.title,diff:c.diff,level:lv,score:Math.min(10000000,Math.round(score)),lamp:lamp||'COMP',artist:c.artist,bpm:c.bpm});else records.delete(c.key);notes.set(c.key,{goal:$('sdvxEditGoal').value==='1',memo:$('sdvxEditMemo').value.trim()});save();$('sdvxEditDialog').close();render()};$('sdvxDeleteOne').onclick=()=>{records.delete(editing);notes.delete(editing);save();$('sdvxEditDialog').close();render()};
+document.querySelectorAll('[data-sview]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-sview]').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('#sdvxApp .moduleView').forEach(x=>x.classList.remove('active'));$('sdvxView'+b.dataset.sview[0].toUpperCase()+b.dataset.sview.slice(1)).classList.add('active');render()});window.rhythmHubRefreshExtras=()=>{refreshHub();try{window.__taikoRefreshHub?.()}catch{}};
+})();
+</script>
+<script>
+/* Taiko no Tatsujin module */
+(()=>{'use strict';
+const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
+const DB='https://raw.githubusercontent.com/taikowiki/taiko-song-database/main/database.json',KM='rhythm-hub:taiko:music:v1',KR='rhythm-hub:taiko:records:v1',KN='rhythm-hub:taiko:notes:v1';
+let charts=[],records=new Map(),notes=new Map(),editing=null;const norm=s=>String(s||'').normalize('NFKC').replace(/\s+/g,' ').trim(),key=(t,d)=>norm(t).toLowerCase()+'|'+d;
+const courseNames={easy:'간단',normal:'보통',hard:'어려움',oni:'오니',ura:'우라'},crownLabel={played:'플레이',silver:'클리어',gold:'풀콤보',donderfull:'전량'};const rec=c=>records.get(c.key),note=c=>notes.get(c.key)||{goal:false,memo:''},yt=c=>'https://www.youtube.com/results?search_query='+encodeURIComponent('太鼓の達人 '+c.title+' '+(c.diff==='ura'?'裏':'おに'));
+function loadLocal(){try{const a=JSON.parse(localStorage.getItem(KM)||'[]');if(Array.isArray(a))charts=a}catch{}try{const a=JSON.parse(localStorage.getItem(KR)||'[]');if(Array.isArray(a))a.forEach(r=>records.set(key(r.title,r.diff),r))}catch{}try{const a=JSON.parse(localStorage.getItem(KN)||'[]');if(Array.isArray(a))a.forEach(n=>notes.set(n.key,{goal:!!n.goal,memo:String(n.memo||'')}))}catch{}}
+function save(){localStorage.setItem(KR,JSON.stringify([...records.values()]));localStorage.setItem(KN,JSON.stringify([...notes.entries()].map(([key,v])=>({key,...v}))));refreshHub()}
